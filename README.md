# Premium Personal Profile

Open `index.html` directly or deploy the folder to Vercel, Netlify, or GitHub Pages.

## Customize
Edit the `CONFIG` object near the bottom of `index.html`. Your name, Discord username, socials, projects, and stats are centralized there.

Replace the placeholder avatar URLs with your own image URL.
